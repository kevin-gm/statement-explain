package site.wellman.sql.explain.core.notifier;

import site.wellman.sql.explain.core.AnalysisResult;

import java.util.logging.Logger;

/**
 * @author li.guoqiang
 **/
public class LogExplainResultNotifier implements ExplainResultNotifier {

    private final static Logger LOGGER = Logger.getLogger(LogExplainResultNotifier.class.getName());

    @Override
    public void notifyResult(AnalysisResult analysisResult) {

    }
}
